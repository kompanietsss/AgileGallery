# AgileGallery

## Getting Started

`git clone`

`yarn` in the root directory

##### Android

In the root directory

`yarn android`

##### iOS

In the `ios` directory:

`pod install`

In the root directory

`yarn ios`
