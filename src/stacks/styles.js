import {StyleSheet} from 'react-native';
import {w} from '../helpers/misc';

export default StyleSheet.create({
  textLeft: {
    color: '#fff',
    fontSize: (4 * w) / 100,
    marginLeft: (3 * w) / 100,
  },
  textRight: {
    color: '#fff',
    fontSize: (4 * w) / 100,
    marginRight: (3 * w) / 100,
  },
});
